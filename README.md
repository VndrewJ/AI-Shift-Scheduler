# GPT-Shift-Scheduler
Uses GPT API to auto-schedule shifts by analysing messages in FB Messenger chat

## TODO
Add 
- Read USER ID to can insert into appropriate user slot, no need to state name
- A way to insert new users in (Probably shouldnt do this actually, only manager should be able to do this)


Refactor
- shift_service.py 


